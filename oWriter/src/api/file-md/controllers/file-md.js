'use strict';

/**
 *  file-md controller
 */

const { createCoreController } = require('@strapi/strapi').factories;

module.exports = createCoreController('api::file-md.file-md',({ strapi }) =>  ({
  // Method 1: Creating an entirely custom action
}));
